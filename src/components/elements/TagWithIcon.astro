---
import TagIcon from '$/components/elements/TagIcon.astro'
import type { Tag } from '$/types'

const { tag } = Astro.props as { tag: Tag }
---

<h3 class="flex gap-4 text-2xl font-bold">
  {tag !== 'other' && <TagIcon tag={tag} />}
  {tag}
</h3>
